;;; config-navigation.el --- -*- lexical-binding: t -*-
;;; Commentary:
;;; Code:

(use-package backward-forward
  :config
  (backward-forward-mode t))

(provide 'config-navigation)
;;; config-navigation.el ends here
